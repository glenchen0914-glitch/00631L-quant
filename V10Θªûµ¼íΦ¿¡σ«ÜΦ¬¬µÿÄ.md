# V10 Auto Final 首次設定

完成一次設定後，平日不需要開桌機、GitHub 或 Colab。

## 一、上傳專案

1. 解壓縮 ZIP。
2. 覆蓋電腦中的 `00631L-quant` Repository。
3. GitHub Desktop 輸入：`Update to V10 Auto Final`
4. Commit to main。
5. Push origin。

## 二、確認自動執行

到 GitHub Repository：

`Actions` → `00631L Auto Final`

按一次 `Run workflow` 做首次測試。

正式排程為台灣時間週一至週五 14:35。GitHub Actions 可能因系統負載延遲數分鐘。

## 三、設定 LINE 推播

未設定 LINE Secrets 時，系統仍會自動執行並更新 GitHub 報告，只是不會傳送 LINE。

需要在 LINE Developers 建立 Messaging API Channel，取得：

- Channel access token
- 你的 User ID

然後到 GitHub：

`Settings` → `Secrets and variables` → `Actions` → `New repository secret`

建立：

- `LINE_CHANNEL_ACCESS_TOKEN`
- `LINE_USER_ID`

設定後，再到 Actions 手動執行一次測試。

## 四、每天怎麼看

最方便方式是看 LINE 推播。

也可以在 GitHub Repository 打開：

- `reports/README.md`：精簡結論
- `reports/dashboard.html`：完整儀表板
- `reports/decision_history.csv`：歷史決策
- Actions 執行頁面的 Artifact：完整報告下載包

## 五、手動重跑

手機或電腦都可進入 GitHub：

`Actions` → `00631L Auto Final` → `Run workflow`

不需要開 Colab。
